import React, { PureComponent } from "react";
import {
  LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, Brush,
  AreaChart, Area,
} from 'recharts';

class Leadergraph extends PureComponent {
}

export default Leadergraph;